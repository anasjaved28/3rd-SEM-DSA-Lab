#include<stdio.h>
#include<stdlib.h>
// define node structure
typedef struct Node{
    int data;
    struct Node* next;
    struct Node* prev;
}Node;

// add node function
void addafter(){
    struct Node* temp, *q;
}